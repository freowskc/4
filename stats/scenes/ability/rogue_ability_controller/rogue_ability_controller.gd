extends Node
#
#const DMG_MAX = 3
#@export var rogue_ability: PackedScene
#@onready var health_component: HealthComponent = %HealthComponent
#
#func _ready():
	#var timer = $"../Timer"
	#timer.timeout.connect(on_timer_timeout)
	#
#
#func on_timer_timeout():
	#var enemy = get_tree().get_first_node_in_group("enemy") as Node2D
	#if enemy == null:
		#return
	
	#health_component.damage(DMG_dealt)
	#rogue_instance.global_position = enemy.global_position
	
	
	
